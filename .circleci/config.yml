version: "2.1"
jobs:
  build:
    docker:
     - image: circleci/node:7.10
       environment:
         CHROME_BIN: "/usr/bin/google-chrome"  # "/Applications/Google Chrome.app"

    steps:
     - checkout
     - run: npm install 
     - run: npm test

  # testing build